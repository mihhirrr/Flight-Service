const zod = require('zod');
const { StatusCodes } = require('http-status-codes');
const { Error } = require('../utils/common-utils')

async function extractFilter(req,res,next) {
    
}




module.exports = {
    extractFilter
}